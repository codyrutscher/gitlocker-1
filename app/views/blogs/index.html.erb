<% if params[:page].present? %>
  <%= content_for :meta_title, "GitLocker Blogs Page #{params[:page]}" %>
  <%= content_for :meta_description, "Discover the latest trends in software engineering with GitLocker's insightful blog posts. Stay ahead in the coding world with expert tips and tutorials Page #{params[:page]}" %>
<% else %>
  <%= content_for :meta_title, "GitLocker Blogs" %>
  <%= content_for :meta_description, "Discover the latest trends in software engineering with GitLocker's insightful blog posts. Stay ahead in the coding world with expert tips and tutorials" %>
<% end %>
<%=
  render BreadcrumbComponent.new(current_user,[
    { text: "Blogs", url: blogs_path }
  ])
%>
<div class="w-full mx-auto bg-white">
  <div class="mx-auto py-6">
    <div class="mx-auto max-w-2xl lg:mx-0">
      <h1 class="text-pretty text-4xl font-semibold tracking-tight text-gray-900 sm:text-5xl pl-8">From the blog</h1>
      <p class="mt-2 text-lg/8 text-gray-600 pl-8">Learn how to grow your business with our expert advice from our <span class="bg-blue-100 text-blue-800 font-semibold text-lg/8 px-2.5 py-0.5 rounded dark:bg-blue-200 dark:text-blue-800"><%= Blog.count %></span> blogs.</p>
    </div>
     <hr class="h-px bg-black border-4 mt-4 mb-4 border-black">
    <div class="grid gap-6 px-8 md:grid-cols-2 lg:grid-cols-6">
      <% @blogs.each do |blog| %>
        <div class="bg-white hover:bounceOrig hover:shadow-[0px_20px_20px_15px_#1158f2] border-4 border-black rounded-lg overflow-hidden">
          <%= link_to blog_path(blog), class: "block" do %>
            <% if blog.image.attached? %>
              <%= image_tag url_for(blog.image), class: "w-full h-48 object-cover", alt: "#{blog.title} Images" %>
            <% end %>
          <hr
  class="my-4 h-px border-t-0 bg-black" />
            <div class="p-4">
              <h2 class="text-xl font-semibold mb-2"><%= blog.title %></h2>
              <p class="text-gray-700 mb-4">
              <% linked_content = blog.content %>
                <%= sanitize(linked_content.truncate(300, separator: ' ')) %>
              </p>
              <hr
  class="my-4 h-px border-t-0 bg-black" />
              <p class="text-md text-black font-bold underline italic">Posted on <%= blog.created_at.strftime("%B %d, %Y") %></p>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="mt-6">
      <%= render 'kaminari/tailwind/paginator', paginator: @blogs %>
    </div>
  </div>
</div>

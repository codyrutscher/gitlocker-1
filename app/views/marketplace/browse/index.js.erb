document.getElementById('products').innerHTML = `<%= j render partial: "home/product", collection: @products %>`;

$("#pagination_div").html("<%= j render 'kaminari/tailwind/paginator', paginator: @products, params: request.query_parameters.merge(page: params[:page]) %>");
